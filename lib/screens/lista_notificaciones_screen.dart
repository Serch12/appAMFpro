import 'package:flutter/material.dart';

class listaNotificacionesScreen extends StatelessWidget {
  const listaNotificacionesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Notificaciones Screen')));
  }
}
