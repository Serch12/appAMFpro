import 'package:flutter/material.dart';

class validacionGifScreen extends StatelessWidget {
  const validacionGifScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Image.asset('assets/validacioninfo.gif')));
  }
}
